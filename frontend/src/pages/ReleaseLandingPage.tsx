import { Link } from 'react-router-dom';
import './LaunchPage.css';
export function ReleaseLandingPage(){return <main className="launch" style={{maxWidth:1080}}><nav className="launch-title"><strong>Meridian</strong><Link to="/login">Sign in ↗</Link></nav><section style={{padding:'90px 0 64px',maxWidth:760}}><div className="launch-eyebrow">FOR YOUR FIRST APP. AND THE NEXT VERSION.</div><h1 style={{fontSize:'clamp(40px,6vw,72px)'}}>You bring the code.<br/>We help you launch.</h1><p style={{fontSize:18,maxWidth:600}}>Upload your project, import a public repository, or connect your coding agent. Check what your app needs, try a preview, and publish when you’re ready.</p><Link to="/signup" style={{display:'inline-block',marginTop:20,padding:'14px 24px',background:'var(--accent)',color:'white',borderRadius:9,textDecoration:'none',fontSize:15}}>Start a project →</Link><p className="launch-muted">Early access · Email sign-in · GitHub optional</p></section><section className="launch-layout" style={{gridTemplateColumns:'repeat(auto-fit,minmax(230px,1fr))'}}>{[['01','Bring your project','Upload a folder or ZIP. Your source stays versioned, so every launch has a history.'],['02','See what it needs','Check the detected runtime and missing variables before building a preview.'],['03','Publish with a way back','Track deployment progress and restore a previously healthy release from your dashboard.']].map(([n,t,d])=><article className="launch-card" key={n}><p className="launch-eyebrow">{n}</p><h2>{t}</h2><p>{d}</p></article>)}</section><p className="launch-muted">Early access supports one web service per project: Node.js, FastAPI, Go, static sites, or a custom Dockerfile. Connect existing databases through environment variables. Hosted capacity must be configured by the Meridian operator.</p></main>}
